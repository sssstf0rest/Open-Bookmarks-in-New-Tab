{
  "name": "open-bookmarks-in-new-tab",
  "version": "2.5.0",
  "private": true,
  "description": "Dependency-free Chrome extension with local HTTP 204 navigation cancellation",
  "engines": { "node": ">=18" },
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "check": "node scripts/check.mjs",
    "build": "node scripts/build.mjs"
  }
}
